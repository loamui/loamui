{
  "$comment": "Behavioural eval definitions, run in clean consumer projects with and without the skill. Grade generated artifacts and browser behaviour; these definitions are not an automated runner. See README.md.",
  "evals": [
    {
      "id": "danger-button",
      "prompt": "Using LoamUI, add a 'Delete account' button to this settings page. It should look like a destructive action.",
      "expect": [
        "Wraps the Button in a region declaring --loam-context: danger (inline style or a scoped class)",
        "Uses <Button> from @loamui/core, not a styled <a> or a div",
        "Does not invent a variant, color, or intent prop"
      ],
      "forbid": ["variant=", "color=\"danger\"", "className=\"loam-Button", "!important"]
    },
    {
      "id": "signup-form",
      "prompt": "Build a sign-up form with LoamUI: name, email, password, and an optional company field, with validation errors.",
      "expect": [
        "Each text control sits inside Field.Root with Field.Label and Field.Error",
        "The optional field uses <Field.Label optional> rather than marking required fields",
        "Field supplies default relationships; any explicit IDs or descriptions resolve correctly without duplicate IDs",
        "Error messages say what to do in the question's words (e.g. 'Enter your email address')"
      ],
      "forbid": ["This field is required", "Please enter"]
    },
    {
      "id": "wide-modal",
      "prompt": "Our LoamUI Modal is too narrow for the release-notes content. Make it wider.",
      "expect": [
        "Sets --loam-modal-size (style prop or scoped CSS) where the modal is used",
        "Does not look for or add a size prop"
      ],
      "forbid": ["size=\"lg\"", "size={", "width: 900px"]
    },
    {
      "id": "cta-link",
      "prompt": "Add a prominent 'Book a demo' call-to-action that links to /demo, styled like the LoamUI buttons.",
      "expect": [
        "Uses SignpostLink (navigation), optionally with render={<Link/>} for a router link",
        "Explains that Button is for actions and a link should not be dressed as a button"
      ],
      "forbid": ["<Button", "className=\"loam-Button", "role=\"button\""]
    },
    {
      "id": "faq-accordion",
      "prompt": "Implement an FAQ accordion with LoamUI.",
      "expect": [
        "Uses Details.Root / Details.Summary / Details.Content per question",
        "Does not hand-roll a details element with loam-Details classes or a custom chevron"
      ],
      "forbid": ["Accordion", "className=\"loam-Details", "<summary>"]
    },
    {
      "id": "theme-brand",
      "prompt": "Rebrand this LoamUI app to a deep green primary colour with a serif display font.",
      "expect": [
        "Overrides --loam-color-primary and --loam-font-display at :root (or a scope), in oklch()",
        "Does not edit component CSS, private --_ properties, or add !important"
      ],
      "forbid": ["#0", "!important", "--_"]
    },
    {
      "id": "nextjs-install",
      "prompt": "Set up LoamUI in this Next.js app.",
      "expect": [
        "Installs @loamui/core with the existing package manager and imports @loamui/core/styles.css in the root layout before application styles, following the installation guide",
        "No provider or plugin is added"
      ],
      "forbid": ["ThemeProvider", "LoamProvider", "sync-loamui-css", "cssMinify: false"]
    },
    {
      "id": "page-layout",
      "prompt": "Lay out a dashboard page with LoamUI: a sidebar and a grid of stat cards.",
      "expect": [
        "Uses native CSS grid/flex with var(--loam-space-*) for gaps, not a layout component",
        "Uses Card for the stat surfaces",
        "Does not look for Grid, Stack, or Container components"
      ],
      "forbid": ["<Grid", "<Stack", "<Container", "<Flex"]
    },
    {
      "id": "unknown-component",
      "prompt": "Use the LoamUI DateInput to collect a date of birth.",
      "expect": [
        "Reads references/components/date-input.md (or the live .md) before writing code",
        "Uses the documented parts and wiring from that reference rather than guessing props"
      ],
      "forbid": ["type=\"date\""]
    },
    {
      "id": "responsive-editorial-grid",
      "prompt": "Use LoamUI to build an editorial section with one large featured article and two smaller articles. I need to reuse it in a full-width page and a narrow sidebar.",
      "expect": [
        "Uses a semantic collection and existing core parts, preserving their surfaces",
        "At 224px and 1000px in a plain parent there is no page overflow",
        "Any size query measures an ancestor of the changing layout, with consistent thresholds for tracks and spans",
        "Links identify their article destinations; no inert buttons or docs-site imports"
      ],
      "forbid": ["!important", "className=\"loam-Card\""]
    },
    {
      "id": "independent-faqs",
      "prompt": "Build a LoamUI FAQ for delivery and another for returns. Show both on the same page.",
      "expect": [
        "Keeps primary FAQ answers visible; uses Details only when there is a reason to hide secondary information",
        "IDs are unique across instances and headings name the correct sections",
        "If disclosures are exclusive, opening delivery does not close returns",
        "Provides keyboard and repeated-instance verification results or states what could not be run"
      ],
      "forbid": []
    },
    {
      "id": "adapt-recipe",
      "prompt": "Adapt the LoamUI Article card recipe for a pottery journal with an author, publication date and a link to read the article.",
      "expect": [
        "Reads the published recipe and relevant component references",
        "Changes the article copy, author, image alternative text, date and destination coherently",
        "Uses a link for article navigation and retains Card surface ownership",
        "Keeps copied React and CSS independent of the docs catalog and preview machinery",
        "Uses tokens for design values while allowing structural dimensions",
        "Checks the reference against the workflow and verifies the adaptation rather than assuming publication certifies conformance",
        "Declares scoped recipe rules in loamui.components and resolves fluid type within its measuring region"
      ],
      "forbid": ["@/renderer/", "@/examples/"]
    },
    {
      "id": "choose-banner-recipe",
      "prompt": "Use LoamUI to add a short seed-sale promotion halfway down an existing page. The photograph should fill the background and there should be one link to the sale.",
      "expect": [
        "Reads the Banner with background image recipe and its When to use guidance",
        "Uses a compact named section with an appropriate heading, one navigation link and a decorative image",
        "Does not turn the promotion into a page-opening hero or force it into the Card primitive",
        "Checks narrow layout and text contrast against the image overlay in the consuming theme"
      ],
      "forbid": ["<h1", "variant=", "fullWidth="]
    },
    {
      "id": "consumer-setup-approval",
      "prompt": "Use LoamUI to add an image hero to this existing React application. Ask me before changing shared setup.",
      "environment": "Next.js App Router or TanStack Start project without @loamui/core, with an unlayered img height:auto rule; existing package manager and app root provided.",
      "expect": [
        "Inspects dependencies, stylesheet order and relevant global CSS before editing",
        "Explains the missing package and specific CSS conflict and proposes minimal affected files",
        "Waits for setup approval; does not migrate unrelated application styling",
        "After approval uses real package exports and verifies image height on a direct load and client navigation"
      ],
      "forbid": ["className=\"loam-Button"]
    },
    {
      "id": "chat-source-only",
      "prompt": "Use LoamUI to make a sign-in form with validation errors.",
      "environment": "No repo, package installation, network or renderer; provide the skill with its bundled references.",
      "expect": [
        "Uses offline component and recipe contracts",
        "Provides real package imports, complete React and CSS plus required integration",
        "States that rendering and runtime tests could not run",
        "Does not emulate core or claim the result is verified"
      ],
      "forbid": []
    },
    {
      "id": "recipe-clipboard",
      "prompt": "Paste the exact generated Hero with image recipe prompt, without extra implementation hints.",
      "environment": "Fresh supported framework project with core, its stylesheet and the LoamUI skill installed; no network. Give the agent the exact short prompt shown on the recipe page.",
      "expect": [
        "Resolves the named recipe in the skill index and reads bundled source and component contracts without extra implementation hints",
        "Checks companion skill availability, Stylelint and composition checks; proposes missing shared setup before installing it",
        "Preserves real LoamUI components and standalone recipe files",
        "Checks layer order and verifies content-driven image containment, parent widths and keyboard behaviour",
        "Reports actual checks and gaps instead of blanket certification"
      ],
      "forbid": ["@/examples/", "@/renderer/"]
    },
    {
      "id": "prepare-project",
      "prompt": "Use the LoamUI skill to set up this project for LoamUI development.",
      "environment": "Run separately in Next.js App Router and TanStack Start applications with @loamui/core installed. Existing formatter and type checker; no CSS lint. Approve the concrete proposal, then repeat the same task.",
      "expect": [
        "Inspects package exports, stylesheet order, existing commands, agent capabilities and browser policy",
        "Explains the missing checks, proposed dependencies and exact configuration files before editing",
        "Uses the maintained skill assets and resolves token references from the installed package, without monorepo paths",
        "Configures the shared scope and spacing checks for actual composition directories alongside Stylelint",
        "Reuses the existing formatter and type checker and scopes CSS lint to actual authored styles",
        "Offers project-local companion skills, reads relevant guidance and records whether they are available",
        "Runs the configured checks and reports failures or unavailable verification accurately",
        "Repeating setup preserves local overrides and does not duplicate scripts or agent instructions"
      ],
      "forbid": ["packages/core/src/tokens.css", "--all", "--global"]
    },
    {
      "id": "existing-checks",
      "prompt": "Use LoamUI to add an article card for a pottery journal. Keep our existing tools.",
      "environment": "Framework app with a formatter, custom Stylelint overrides, local brand tokens, and browser tests. LoamUI and its styles are installed.",
      "expect": [
        "Reuses existing check commands and reads article-card and Card contracts",
        "Does not replace config files, install a second formatter or drop local token references",
        "Builds the requested card with semantic content, scoped layered CSS and real LoamUI exports",
        "Runs relevant checks and repairs failures without weakening the rules"
      ],
      "forbid": ["!important", "className=\"loam-Card\""]
    },
    {
      "id": "client-landing-page",
      "prompt": "Build the marketing homepage for Fernbank, an independent garden nursery in Devon: hero, what we grow, opening times and directions, and a way to ask about a plant. Use LoamUI.",
      "expect": [
        "Writes a design plan before code: subject and audience, palette, typefaces, layout concept, the one memorable thing",
        "Sets the token inputs in a theme stylesheet (--loam-color-primary and neutrals as light-dark oklch pairs, --loam-font and --loam-font-display), not the defaults",
        "Loads the chosen typefaces with a fallback stack, through the framework's font mechanism or a stylesheet link",
        "Assembles the page from recipes and components (a hero, a grid or cards, a form inside Field, Nav with the current link marked, SkipLink) in a purposeful order",
        "Section gaps come from the space scale and are not all the same size; prose stops at the measure",
        "Reports a visual review in both colour schemes at narrow and wide widths alongside the checks that ran, and the contrast pairs measured for the custom palette",
        "Copy is specific to the nursery, not filler"
      ],
      "forbid": ["font-size: 48px", "#F4F1EA", "variant=", "className=\"loam-", "!important", "Lorem ipsum"]
    }
  ]
}
